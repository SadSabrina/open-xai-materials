.DS_Store
__pycache__/
*.pyc
.ipynb_checkpoints/

# Данные курса лежат в репозитории намеренно (правило 1.3: датасет рядом с ноутбуком).
# Глобальный ~/.gitignore_global содержит *.csv — из-за него data/orange_dataset.csv
# молча не попал в репозиторий, и HW3 отдавал 404. Это правило перебивает глобальное.
!data/**
